import React from 'react';
import { observer } from 'mobx-react';
import UserPage from '../../../common/organisms/userPage/userPage';

const userPageView = observer(() => {
    
    
    return (
        <UserPage />
    );
});

export default userPageView;